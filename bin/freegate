#!/bin/sh

FG="$HOME/freegate/fg723p.exe"
if [ -f "$FG" ]; then
    if [ -f "$(which wine)" ]; then
        nohup wine $FG >/dev/null 2>&1 &
    else
        echo 'Wine loader not install yet.'
    fi
else
    echo 'freegate.exe not found.'
fi
